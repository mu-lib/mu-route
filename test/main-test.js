define([
  "buster",
  "../main"
], function (buster, Router) {
  "use strict";

  var assert = buster.referee.assert;
  var refute = buster.referee.refute;

  buster.testCase("mu-route/main", {
  });
});
